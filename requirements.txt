# SWLP — Sliding Window Layer Pipeline
# Install: pip install -r requirements.txt
#
# Or use extras for optional features:
#   pip install swlp[apple]   — MLX backend (Apple Silicon)
#   pip install swlp[gpu]     — NVIDIA VRAM tracking
#   pip install swlp[dev]     — pytest + ruff

# ── Core dependencies ──────────────────────────────────────────────────────────
torch==2.12.0
transformers==5.8.1
accelerate==1.13.0
huggingface-hub==1.15.0
safetensors==0.7.0
python-json-logger==4.1.0
psutil==7.2.2
pyinstrument==5.1.2

# ── Dev dependencies (testing + linting) ───────────────────────────────────────
pytest==9.0.3
pytest-cov==7.1.0
ruff==0.15.13

# ── Optional: Apple Silicon MLX backend ────────────────────────────────────────
# Uncomment if you are on Apple Silicon and want --backend mlx (~16 tok/s)
# mlx==0.31.2
# mlx-lm==0.31.3

# ── Optional: NVIDIA VRAM tracking ─────────────────────────────────────────────
# Uncomment if you have an NVIDIA GPU and want VRAM usage reported
# pynvml>=11.5

# ── Install SWLP itself in editable mode ───────────────────────────────────────
-e .
